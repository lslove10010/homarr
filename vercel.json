{
  "framework": "nextjs",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "rootDirectory": "apps/web", // 若 Next.js 在子目录，替换为实际路径；否则删掉
  "functions": {
    "api/**/*": {
      "maxDuration": 10
    }
  },
  "env": {
    "NPM_CONFIG_PACKAGE_MANAGER": "pnpm",
    "NODE_ENV": "production"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
